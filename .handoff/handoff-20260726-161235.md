# DeepFaceLab TF2.x 项目交接文档：Batch 1 首轮实现与 Ticket 化

> 交接编号：H-003
> 创建日期：2026-07-26
> 仓库：`twj515895394/deep-face-lab-tf2.x`
> 默认分支：`main`
> 上一份交接：`handoff-20260726-batch1-detailed-design.md`
> 本次定位：完成 Batch 1 的本地 ticket 拆分、macOS 轻量 smoke harness、Eyes / Mouth Priority 真实 mask 修复，并确立 Python 3.9+ 基线。

---

## 1. 本次会话目标

承接 H-002 中的下一步：

```text
B1-00：冻结基线与建立最小 smoke harness
B1-01：修复 Eyes / Mouth Priority 真实 mask
```

同时根据用户要求：

- 将 Batch 1 进一步拆成本地 Markdown tickets；
- 在 Codex App 中评估可分发任务并使用后台 Worker 做只读审计；
- 按 macOS 无 GPU 的现实约束，只做轻量编译、导入、纯函数和 mock 级验证；
- 将项目 Python 基线调整为最低 3.9，推荐 3.11 / 3.12。

---

## 2. 已完成工作

### 2.1 拉取与入口确认

本地 `main` 已从 `github/main` 快进到 H-002 之后的最新文档状态。`gitee/main` 在当时未包含 GitHub 侧新增文档提交。

读取并确认：

```text
.handoff/current.md
.handoff/handoff-20260726-batch1-detailed-design.md
docs/development/batch1-correctness-and-extension-foundation-tasks.md
docs/agents/issue-tracker.md
```

### 2.2 建立 Batch 1 本地 ticket

新增本地追踪目录：

```text
.scratch/batch1-correctness-foundation/
```

其中：

- `spec.md`：Batch 1 ticket 总入口、平台验证约定、frontier；
- `issues/01-...` 到 `issues/11-...`：11 个独立 ticket；
- `reports/01-...summary.md` 与 `reports/02-...summary.md`：已完成 ticket 总结。

当前 frontier 见：

```text
.scratch/batch1-correctness-foundation/spec.md
```

### 2.3 完成 Ticket 01：macOS 轻量 smoke harness

新增：

```text
tools/smoke/batch1_mac_smoke.py
tests/smoke/test_batch1_mac_smoke.py
```

能力：

- 记录 Git commit、branch、Python、平台和关键依赖；
- 运行 AST 语法扫描；
- 记录轻量导入失败原因；
- 明确 macOS 不验证 GPU 训练；
- 输出 Windows GPU 必补验证项。

默认运行产物写入：

```text
workspace/validation/batch1/<timestamp>/
```

该目录已由 `.gitignore` 排除，不应提交运行日志。

### 2.4 完成 Ticket 02：Eyes / Mouth Priority 真实 mask 修复

修改：

```text
models/Model_SAEHD/Model.py
tests/smoke/test_batch1_eyes_mouth_masks.py
```

要点：

- `self._has_eyes_mouth` 提前到训练图构建前设置；
- `eyes_mouth_prio=False` 保持 src/dst 各 3 输出旧路径；
- `eyes_mouth_prio=True` 要求 src/dst 各 4 输出，并读取第 4 个 EYES_MOUTH mask；
- 真实 EM mask 传入 `unified_train()`，不再正常路径使用 `np.zeros_like(...)`；
- 缺 mask、shape 不一致、dtype 不安全、inf/nan 均明确失败；
- 增加 mock/纯函数测试覆盖 `onTrainOneIter()` 到 `unified_train()` 的真实传参。

### 2.5 Python 版本基线调整

用户明确确认项目最低 Python 为 3.9，推荐 3.11 / 3.12。

已同步：

```text
main.py
tools/smoke/batch1_mac_smoke.py
tests/smoke/test_batch1_mac_smoke.py
docs/development/batch1-correctness-and-extension-foundation-tasks.md
.scratch/batch1-correctness-foundation/spec.md
.scratch/batch1-correctness-foundation/reports/*
```

当前 smoke 输出中会记录：

```text
python.minimum_supported = "3.9"
```

---

## 3. 模型路由与审计情况

本轮使用 `codex-model-routing-team` 做过分发评估。

实际派发的 Worker 均为 OpenAI 基线模型，只读任务，均已归档：

- Luna：Ticket 01 smoke harness 审计；
- Sol：Ticket 02 EM mask 修复审计；
- Luna：规范轴 code review；
- Sol：需求轴 code review。

采纳的关键反馈：

- 不能只修 feed，必须修 `_has_eyes_mouth` 赋值时机；
- smoke harness 不应只 `find_spec()`，要记录真实轻量导入失败原因；
- Python 最低版本改为 3.9 后，可恢复现代类型标注；
- 需要补 `onTrainOneIter()` 到 `unified_train()` 的真实传参 mock 测试；
- 需要用轻量 NumPy 公式证明真实 EM mask 可产生非零 priority contribution。

---

## 4. 验证结果

macOS 本机验证通过：

```bash
python3 -m unittest discover -s tests/smoke -p 'test_batch1_*.py'
python3 -m py_compile main.py models/Model_SAEHD/Model.py tools/smoke/batch1_mac_smoke.py tests/smoke/test_batch1_mac_smoke.py
python3 -m tools.smoke.batch1_mac_smoke --print-json
```

最近一次 smoke 事实：

```text
Python: 3.12.8
minimum_supported: 3.9
platform: Darwin arm64
tests: 13 passed
AST scan: 166 Python files, 0 syntax errors
numpy: available
cv2: missing
tensorflow: missing
core.leras.nn: missing colorama
models / merger.MergeMasked: missing cv2
```

解释：

- 这些缺失是 macOS 当前开发环境事实记录，不代表 Windows GPU 路径失败；
- 真实训练、CUDA/cuDNN、保存恢复和 Merge 仍需 Windows GPU 验证。

---

## 5. 当前 Git 状态与提交

当前 `main` 最新提交：

```text
19549f3 docs(batch1): document python 3.9 baseline
aa3a96f chore(runtime): require python 3.9
abcf7e8 docs(batch1): add correctness foundation tickets
3379c16 fix(training): wire real eyes mouth masks
1d24a54 docs: finalize Batch 1 design handoff status
```

提交拆分符合本仓库约定：

- 代码提交在前；
- 文档提交在 tip。

重要推送提醒：

```text
如需 push，默认只推代码 commit，不推 .scratch / docs / handoff 文档 commit，
除非用户明确授权推送文档。
```

当前从本地视角看，代码相关最新 commit 是：

```text
aa3a96f chore(runtime): require python 3.9
```

但 `3379c16` 是本轮主要训练修复提交；`aa3a96f` 是运行时 Python 基线调整提交。

---

## 6. 当前 Frontier

下一轮可从以下 ticket 中选择：

```text
.scratch/batch1-correctness-foundation/issues/03-training-exception-semantics.md
.scratch/batch1-correctness-foundation/issues/04-precision-contract-audit.md
.scratch/batch1-correctness-foundation/issues/08-enhancement-feature-flags.md
```

推荐顺序：

1. `03-training-exception-semantics.md`
   - 直接承接当前 `onTrainOneIter()` 里非 OOM 异常可能被吞掉的问题；
   - 与已完成的 EM mask 校验关系紧密；
   - 修改范围相对集中。
2. `04-precision-contract-audit.md`
   - 只做观测和审计，不急着重构 FP16 / BF16；
   - 后续 `05/06/07` 依赖该事实基础。
3. `08-enhancement-feature-flags.md`
   - 可与 04 并行，但要注意避免提前引入新 loss 或 Shape-aware Merge。

---

## 7. 建议的 Skill

下一位 Agent 建议按任务选择：

- `implement`：继续执行当前 frontier ticket；
- `codex-model-routing-team`：仅用于只读审计、并行验证或互不重叠的任务，不要让多个 Worker 同时写 `models/Model_SAEHD/Model.py`；
- `code-review`：完成每个 ticket 后按规范轴 / 需求轴审查；
- `diagnosing-bugs`：如果进入训练异常语义、数值异常或低精度问题排查；
- `tdd`：Ticket 03 / 04 可优先用 mock 或小图测试驱动；
- `handoff`：每个重要阶段结束后生成新交接，并更新 `.handoff/current.md`。

---

## 8. 注意事项

- macOS 无 GPU，不要把真实训练、GPU、视觉质量或 Merge 质量写成已完成。
- `eyes_mouth_prio=True` 后真实 loss 可能变化，这是修复生效的预期现象。
- 当前 `onTrainOneIter()` 异常处理仍存在后续 ticket 要修的问题，不要误认为 Batch 1 P0 已全部完成。
- FP16 / BF16 仍不得标记为 validated，直到有 dtype、optimizer slot、保存恢复和 Windows GPU 证据。
- 不要引入 Region / Boundary / Frequency Loss、Dataset Metadata、Identity Geometry、Source Shape Template、Shape-aware Merge、UI 或服务化改造。
