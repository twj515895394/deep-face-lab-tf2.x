# DeepFaceLab TF2.x 项目交接文档：Ticket 04 / 08

> 交接编号：H-005
> 创建日期：2026-07-26
> 仓库：`twj515895394/deep-face-lab-tf2.x`
> 默认分支：`main`
> 上一份交接：`handoff-20260726-165523.md`
> 本次定位：完成 Batch 1 Ticket 04 与 Ticket 08 的 macOS 轻量实现与验证。

---

## 1. 本次会话目标

承接 H-004 frontier：

```text
Ticket 04：建立 Precision Contract 与 dtype 审计
Ticket 08：建立向后兼容的 Enhancement Feature Flag 骨架
```

本轮只建立审计、配置骨架和默认关闭语义，不引入新 loss、采样、Shape-aware Merge、UI 或服务化。

---

## 2. 模型路由执行情况

本轮按 `codex-model-routing-team` 执行本地模型预检与后台 Worker 分工。

### 2.1 本地模型预检

使用主 Codex 本机脚本访问 opencodex 本地 gateway：

```text
probe_url: http://127.0.0.1:10100/v1/responses
```

结果：

```text
cli-proxy/grok-4.5 high: route_eligible=true, semantic_probe=pass
cli-proxy/gemini-3.6-flash-high high: route_eligible=true, semantic_probe=pass
local-qwen/local-qwen3.6-27b high: route_eligible=true, semantic_probe=pass
```

`localhost:8317` 裸连不可达，但本轮以 opencodex 本地 gateway 与 catalog 预检为准。

### 2.2 Worker

创建了两个隔离 worktree Worker：

```text
task_id: B1-T04-GROK-20260726-1715
model: cli-proxy/grok-4.5
thinking: high
thread_id: 019f9db5-27f4-7202-b542-3e42baf6c747
task_intent: mutate
mutation_authority: isolated-worktree
```

```text
task_id: B1-T08-GEMINI-20260726-1715
model: cli-proxy/gemini-3.6-flash-high
thinking: high
thread_id: 019f9db5-3fb6-7200-9028-b2e3108c388c
task_intent: mutate
mutation_authority: isolated-worktree
```

主 Agent 采纳方向但没有直接整包搬运 Worker diff。未采纳 Worker 对历史 handoff、Ticket 03 和 spec 的越界重写。

审计 ledger：

```text
.scratch/batch1-correctness-foundation/reports/model-routing-ledger-ticket04-08-20260726.json
```

---

## 3. 已完成代码修改

新增：

```text
core/leras/precision_contract.py
core/enhancements/__init__.py
core/enhancements/config.py
tests/smoke/test_batch1_precision_contract.py
tests/smoke/test_batch1_config_defaults.py
```

修改：

```text
models/Model_SAEHD/Model.py
.scratch/batch1-correctness-foundation/spec.md
.scratch/batch1-correctness-foundation/issues/04-precision-contract-audit.md
.scratch/batch1-correctness-foundation/issues/08-enhancement-feature-flags.md
.scratch/batch1-correctness-foundation/reports/04-precision-contract-audit-summary.md
.scratch/batch1-correctness-foundation/reports/08-enhancement-feature-flags-summary.md
```

Ticket 04 要点：

- 新增 requested/effective precision contract。
- 新增 dtype audit report 能力，覆盖 weight、gradient、optimizer slot、save/load dtype 字段。
- FP32 是 Batch 1 validated baseline。
- FP16/BF16 保持 experimental，不声明低精度训练已验证。
- SAEHD 异常上下文优先读取 effective precision。

Ticket 08 要点：

- 新增 `EnhancementConfig` 与 `normalize_enhancement_config()`。
- `None` / 空 dict / 旧模型缺字段时所有增强默认关闭。
- 高版本 schema 默认关闭增强并 warning。
- 新模型或已有 `enhancements` 字段时写入规范化 dict。
- Eyes / Mouth Priority 不套 enhancement 总开关。

---

## 4. 测试与验证

macOS 本机验证通过：

```bash
python3 -m unittest tests.smoke.test_batch1_precision_contract tests.smoke.test_batch1_config_defaults
python3 -m unittest discover -s tests/smoke -p 'test_batch1_*.py'
python3 -m py_compile main.py models/Model_SAEHD/Model.py tools/smoke/batch1_mac_smoke.py tests/smoke/test_batch1_mac_smoke.py tests/smoke/test_batch1_eyes_mouth_masks.py tests/smoke/test_batch1_precision_contract.py tests/smoke/test_batch1_config_defaults.py core/leras/precision_contract.py core/enhancements/config.py core/enhancements/__init__.py
python3 -m tools.smoke.batch1_mac_smoke --print-json
python3 /Users/tangwujun/.agents/skills/custom/codex-model-routing-team/scripts/validate_team_ledger.py .scratch/batch1-correctness-foundation/reports/model-routing-ledger-ticket04-08-20260726.json
```

最近一次结果：

```text
18 focused tests passed
35 Batch 1 smoke tests passed
py_compile passed
smoke status: pass
AST scan: 171 Python files, 0 syntax errors
ledger_valid: true
```

当前 macOS 环境事实：

```text
Python: 3.12.8
platform: Darwin arm64
numpy: available
cv2: missing
tensorflow: missing
core.leras.nn: missing colorama
models / merger.MergeMasked: missing cv2
```

真实 GPU 训练、CUDA/cuDNN、低精度 dtype、保存恢复和 Merge 质量仍需 Windows GPU 验证。

---

## 5. Ticket 状态

已更新：

```text
.scratch/batch1-correctness-foundation/issues/04-precision-contract-audit.md
.scratch/batch1-correctness-foundation/issues/08-enhancement-feature-flags.md
.scratch/batch1-correctness-foundation/reports/04-precision-contract-audit-summary.md
.scratch/batch1-correctness-foundation/reports/08-enhancement-feature-flags-summary.md
```

状态：

```text
Ticket 04: done-macos-lightweight
Ticket 08: done-macos-lightweight
```

---

## 6. 当前 Frontier

下一轮建议：

```text
Ticket 05：建立 optimizer roundtrip 审计基础
Ticket 10：建立 Merge 默认路径 smoke
```

推荐顺序：

1. `05-optimizer-roundtrip-audit.md`
   - 继续承接 precision contract，记录 optimizer slot dtype 与 roundtrip 数值误差；
   - 不在 05 中修 Lion 公式。
2. `10-merge-default-path-smoke.md`
   - 验证缺失 Enhancement Config 时 Merge 默认路径不变；
   - 不实现 Shape-aware Merge。

---

## 7. 注意事项

- Ticket 07 finite gradient gate 仍未实现；当前 inf/nan 检测仍不应写成 pre-update gate 已完成。
- Ticket 06 Lion v2 依赖 Ticket 05，仍未实现。
- Ticket 09 保存恢复 smoke 仍 blocked by 06 / 07。
- 本轮没有提交 commit，没有 push。
- push 前必须遵守仓库规则：只在用户明确确认后执行，且默认不推文档 commit。
