# DeepFaceLab TF2.x 项目交接文档：Ticket 05 / 10

> 交接编号：H-006
> 创建日期：2026-07-26
> 仓库：`twj515895394/deep-face-lab-tf2.x`
> 默认分支：`main`
> 上一份交接：`handoff-20260726-174706.md`
> 本次定位：完成 Batch 1 Ticket 05 与 Ticket 10 的 macOS 轻量实现与验证。

---

## 1. 本次会话目标

承接 H-005 frontier：

```text
Ticket 05：建立 optimizer roundtrip 审计基础
Ticket 10：建立 Merge 默认路径 smoke
```

本轮只建立审计与 smoke，不修 Lion 公式，不实现 Shape-aware Merge，不引入新 loss、sampling、identity geometry、UI 或服务化。

---

## 2. 模型路由执行情况

本轮按 `codex-model-routing-team` 执行本地模型预检与后台 Worker 分工。

### 2.1 本地模型预检

使用主 Codex 本机脚本访问 opencodex 本地 gateway：

```text
probe_url: http://127.0.0.1:10100/v1/responses
```

结果：

```text
cli-proxy/grok-4.5 high: route_eligible=true, semantic_probe=pass
cli-proxy/gemini-3.6-flash-high high: route_eligible=true, semantic_probe=pass
```

### 2.2 Worker

创建了两个隔离 worktree Worker：

```text
task_id: B1-T05-GROK-20260726-1806
model: cli-proxy/grok-4.5
thinking: high
thread_id: 019f9de6-2722-72d3-8796-3fdbb056ba1d
task_intent: mutate
mutation_authority: isolated-worktree
adopted: true
```

```text
task_id: B1-T10-GROK-20260726-1806
model: cli-proxy/grok-4.5
thinking: high
thread_id: 019f9de7-7076-7601-93c5-f93ee7cbaf92
task_intent: mutate
mutation_authority: isolated-worktree
adopted: true
```

主 Agent 审查后采纳 Worker 代码方向，并在主工作区重新运行验证。

审计文件：

```text
.scratch/batch1-correctness-foundation/reports/route-plan-ticket05-20260726.json
.scratch/batch1-correctness-foundation/reports/route-plan-ticket10-20260726.json
.scratch/batch1-correctness-foundation/reports/model-routing-ledger-ticket05-10-20260726.json
```

---

## 3. 已完成代码修改

新增：

```text
core/leras/optimizer_roundtrip.py
tests/smoke/test_batch1_optimizer_roundtrip.py
tests/smoke/test_batch1_merge_default_path.py
```

修改：

```text
core/leras/precision_contract.py
.scratch/batch1-correctness-foundation/spec.md
.scratch/batch1-correctness-foundation/issues/05-optimizer-roundtrip-audit.md
.scratch/batch1-correctness-foundation/issues/10-merge-default-path-smoke.md
.scratch/batch1-correctness-foundation/reports/05-optimizer-roundtrip-audit-summary.md
.scratch/batch1-correctness-foundation/reports/10-merge-default-path-smoke-summary.md
```

Ticket 05 要点：

- 新增 NumPy optimizer roundtrip 轻量 harness。
- 覆盖 AdaBelief、RMSprop、Lion 的连续训练 vs 保存恢复后下一步更新。
- 记录 optimizer slot dtype、reload error 与 update error。
- 扩展 precision dtype audit，使其识别真实 leras optimizer 的 dict slot 属性。
- 不修改 Lion 公式；当前 beta2 不影响轨迹被记录为 legacy 事实。

Ticket 10 要点：

- 新增 Merge 默认路径 smoke。
- 通过 dummy predictor + 最小 landmarks fixture 覆盖 `MergeMaskedFace` 默认 overlay 和 `original` 路径。
- 验证缺失 / 全关 Enhancement Config 时传统 Merge 输出一致。
- 不修改 `merger/MergeMasked.py` / `merger/MergerConfig.py` 算法逻辑。
- 不实现 Shape-aware Merge。

---

## 4. 测试与验证

macOS 本机验证通过：

```bash
python3 -m unittest tests.smoke.test_batch1_optimizer_roundtrip -v
python3 -m unittest tests.smoke.test_batch1_merge_default_path -v
python3 -m py_compile core/leras/optimizer_roundtrip.py core/leras/precision_contract.py tests/smoke/test_batch1_optimizer_roundtrip.py tests/smoke/test_batch1_merge_default_path.py
python3 -m unittest discover -s tests/smoke -p 'test_batch1_*.py' -v
python3 -m tools.smoke.batch1_mac_smoke --print-json
python3 /Users/tangwujun/.agents/skills/custom/codex-model-routing-team/scripts/validate_team_ledger.py .scratch/batch1-correctness-foundation/reports/model-routing-ledger-ticket05-10-20260726.json
```

最近一次结果：

```text
Ticket 05 focused tests: 6 passed
Ticket 10 focused tests: 7 passed
Batch 1 smoke tests: 49 passed
py_compile passed
mac smoke status: pass
AST scan: 174 Python files, 0 syntax errors
ledger_valid: true
```

当前 macOS 环境事实：

```text
Python: 3.12.8
platform: Darwin arm64
numpy: available
cv2: missing
tensorflow: missing
core.leras.nn: missing colorama
models / merger.MergeMasked: missing cv2
```

真实 GPU 训练、CUDA/cuDNN、低精度 dtype、真实 TensorFlow optimizer state 保存恢复和 Merge 质量仍需 Windows GPU 验证。

---

## 5. Ticket 状态

已更新：

```text
.scratch/batch1-correctness-foundation/issues/05-optimizer-roundtrip-audit.md
.scratch/batch1-correctness-foundation/issues/10-merge-default-path-smoke.md
.scratch/batch1-correctness-foundation/reports/05-optimizer-roundtrip-audit-summary.md
.scratch/batch1-correctness-foundation/reports/10-merge-default-path-smoke-summary.md
```

状态：

```text
Ticket 05: done-macos-lightweight
Ticket 10: done-macos-lightweight
```

---

## 6. 当前 Frontier

下一轮建议：

```text
Ticket 06：修复 Lion v2 公式并保护 legacy state
Ticket 07：收敛 finite gradient gate 与 Loss Scaling 策略
```

推荐顺序：

1. `06-lion-v2-and-legacy-state.md`
   - 基于 Ticket 05 roundtrip harness 修复标准 Lion beta1/beta2 语义；
   - 明确 legacy Lion optimizer state 不静默恢复为新公式 state。
2. `07-finite-gradient-gate-and-loss-scale-policy.md`
   - 进入实现前需要人工确认低精度策略边界；
   - 建议本批次先收敛策略和 finite gate，FP16/BF16 继续保持 experimental，避免提前推进 FP32 master weight 大改造。

---

## 7. 注意事项

- Ticket 06 会改变 Lion optimizer 语义，必须保护 legacy state。
- Ticket 07 是 HITL，需要用户先确认是否只收敛策略并保留低精度 experimental。
- Ticket 09 保存恢复 smoke 仍 blocked by 06 / 07。
- Ticket 11 Batch 1 compatibility matrix 仍 blocked by 09 / 10。
- 本轮尚未提交 commit，没有 push。
- push 前必须遵守仓库规则：只在用户明确确认后执行，且默认不推文档 commit。
