# DeepFaceLab TF2.x 项目交接文档：Ticket 09

> 交接编号：H-009  
> 创建日期：2026-07-26  
> 仓库：`twj515895394/deep-face-lab-tf2.x`  
> 默认分支：`main`  
> 上一份交接：`handoff-20260726-195000.md`  
> 本次定位：完成 Batch 1 Ticket 09 的 macOS 轻量实现与验证。

---

## 1. 本次会话目标

承接 H-008 frontier：

```text
Ticket 09：建立训练保存恢复 smoke
```

目标是在 macOS 无 TensorFlow/GPU 的限制下，建立可重复的轻量保存恢复 smoke，覆盖训练、保存、销毁、重载和继续 step 的一致性，并明确 Windows GPU 真实 SAEHD 验证边界。

---

## 2. 模型路由执行情况

本轮按 `codex-model-routing-team` 执行。用户明确说明 Grok 已限额，因此 RoutePlan 直接使用 Gemini 备用模型：

```text
task_id: B1-T09-GEMINI-IMPL-20260726-2003
model: cli-proxy/gemini-3.6-flash-high
thinking: high
thread_id: 019f9e51-a201-7fb0-90f8-e05441c68597
status: completed_without_final_message
adopted: false
```

RoutePlan 与 ledger：

```text
.scratch/batch1-correctness-foundation/reports/route-plan-ticket09-20260726.json
.scratch/batch1-correctness-foundation/reports/model-routing-ledger-ticket09-20260726.json
```

说明：

- registry 与 RoutePlan 校验通过。
- 本地 `127.0.0.1:8317` / `127.0.0.1:10100` preflight 当时未监听，因此 runtime 证据来自 Codex App 成功实体化 Worker。
- Worker 线程完成，但 `read_thread` 未暴露 final assistant message，且隔离 worktree 无可采纳 diff；本轮最终采用主 Agent 实现。

---

## 3. 已完成代码修改

修改：

```text
core/leras/training_save_resume_smoke.py
tools/smoke/batch1_mac_smoke.py
tests/smoke/test_batch1_training_save_resume_smoke.py
tests/smoke/test_batch1_mac_smoke.py
.scratch/batch1-correctness-foundation/spec.md
.scratch/batch1-correctness-foundation/issues/09-training-save-resume-smoke.md
.scratch/batch1-correctness-foundation/reports/09-training-save-resume-smoke-summary.md
.scratch/batch1-correctness-foundation/reports/route-plan-ticket09-20260726.json
.scratch/batch1-correctness-foundation/reports/model-routing-ledger-ticket09-20260726.json
```

Ticket 09 要点：

- 新增 `training_save_resume_smoke.py`，用 NumPy 小向量模拟训练、保存、销毁、重载、继续 step。
- 覆盖 AdaBelief、RMSprop、Lion v2 的主权重与 optimizer state 保存恢复。
- 保存 checkpoint 文件并验证存在且非空。
- 缺失 `enhancements` 的 legacy options 继续归一化为全部增强关闭。
- macOS 缺 TensorFlow/GPU 时 FP16/BF16 仍记录为 blocked/experimental，不提升为 validated。
- `batch1_mac_smoke` summary 新增 `checks.training_save_resume`。

---

## 4. 测试与验证

macOS 本机验证通过：

```bash
python3 -m unittest tests.smoke.test_batch1_training_save_resume_smoke -v
python3 -m unittest tests.smoke.test_batch1_training_save_resume_smoke tests.smoke.test_batch1_mac_smoke -v
python3 -m unittest discover -s tests/smoke -p 'test_batch1_*.py' -v
python3 -m py_compile core/leras/training_save_resume_smoke.py tools/smoke/batch1_mac_smoke.py tests/smoke/test_batch1_training_save_resume_smoke.py tests/smoke/test_batch1_mac_smoke.py
python3 -m tools.smoke.batch1_mac_smoke --print-json
```

结果：

```text
Batch 1 smoke: 73 tests passed
training_save_resume optimizers: adabelief, rmsprop, lion
max_abs_reload_error: 0.0
max_abs_update_error: 0.0
Python: 3.12.8
platform: Darwin arm64
tensorflow: missing
cv2: missing
core.leras.nn: missing colorama
models / merger.MergeMasked: missing cv2
```

真实 TensorFlow session、CUDA/cuDNN、Windows GPU 短训、BF16 稳定性、真实 SAEHD 保存恢复和 Merge 质量仍需 Windows GPU 验证。

---

## 5. Ticket 状态

已更新：

```text
.scratch/batch1-correctness-foundation/issues/09-training-save-resume-smoke.md
.scratch/batch1-correctness-foundation/reports/09-training-save-resume-smoke-summary.md
.scratch/batch1-correctness-foundation/spec.md
```

状态：

```text
Ticket 09: done-macos-lightweight
```

---

## 6. 当前 Frontier

下一轮建议：

```text
Ticket 11：汇总 Batch 1 兼容矩阵与 handoff
```

Ticket 11 现在可继续，因为 Ticket 09 / 10 的 macOS 轻量依赖已完成。

---

## 7. 注意事项

- 不要把本轮 NumPy lightweight smoke 写成真实 TensorFlow / SAEHD / GPU 保存恢复已验证。
- FP16 / BF16 仍不得标记为 validated。
- Windows GPU 需要按 summary 中 10 项人工清单补证。
- 本轮尚未 push。push 前必须遵守仓库规则：只在用户明确确认后执行，且默认不推文档 commit。
