# DeepFaceLab TF2.x 项目交接文档：Ticket 11

> 交接编号：H-010  
> 创建日期：2026-07-26  
> 仓库：`twj515895394/deep-face-lab-tf2.x`  
> 默认分支：`main`  
> 上一份交接：`handoff-20260726-201010.md`  
> 本次定位：完成 Batch 1 Ticket 11 的兼容矩阵、复核与 handoff 汇总。

---

## 1. 本次会话目标

承接 H-009 frontier：

```text
Ticket 11：汇总 Batch 1 兼容矩阵与 handoff
```

目标是把 Batch 1 的实际代码状态、macOS 轻量验证、Windows GPU 待验证项、兼容矩阵和后续风险写入文档，供后续开发者判断是否进入 Batch 2。

---

## 2. 模型路由执行情况

本轮按 `codex-model-routing-team` 执行。用户明确说明 Grok 已限额，并要求主要做全面复核阶段，因此 RoutePlan 直接使用 Gemini 备用模型做只读复核：

```text
task_id: B1-T11-GEMINI-REVIEW-20260726-2031
model: cli-proxy/gemini-3.6-flash-high
thinking: high
thread_id: 019f9e6a-564f-75b3-8fd3-a55337e9aa39
status: completed
adopted: partially
```

RoutePlan 与 ledger：

```text
.scratch/batch1-correctness-foundation/reports/route-plan-ticket11-20260726.json
.scratch/batch1-correctness-foundation/reports/model-routing-ledger-ticket11-20260726.json
```

说明：

- registry 与 RoutePlan 校验通过。
- Worker 为 `inspect` / `mutation_authority=none`，没有写入文件。
- Worker 输出用于挑战式复核；最终事实、测试输出、文档写入和 commit 由主 Agent 完成。
- Worker 文本中的 AST 扫描数量为 174；主 Agent 本轮实测为 177，最终采用 177。

---

## 3. 已完成文档修改

新增：

```text
.scratch/batch1-correctness-foundation/reports/11-batch1-compatibility-matrix-and-handoff-summary.md
.scratch/batch1-correctness-foundation/reports/route-plan-ticket11-20260726.json
.scratch/batch1-correctness-foundation/reports/model-routing-ledger-ticket11-20260726.json
.handoff/handoff-20260726-203448.md
```

更新：

```text
.scratch/batch1-correctness-foundation/issues/11-batch1-compatibility-matrix-and-handoff.md
.scratch/batch1-correctness-foundation/spec.md
.handoff/current.md
docs/README.md
```

本轮没有新增运行时接口、训练算法、Merge 算法、CLI 参数或生产配置。

---

## 4. Batch 1 当前结论

Batch 1 的 P0 正确性修复、兼容配置骨架、optimizer / precision 可观测性、训练保存恢复 smoke 与 Merge 默认路径 smoke 已完成 macOS 轻量实现与复核。

当前可以进入 Batch 2 的设计和轻量开发准备，但不能把 Batch 1 宣称为 Windows GPU / TensorFlow / SAEHD 真实训练验收完成。

---

## 5. 兼容矩阵摘要

| 场景 | 当前结论 | Windows GPU 待验证 |
|---|---|---|
| 旧模型无 `enhancements` | 默认全部增强关闭 | 旧 SAEHD `data.dat` 真实加载 |
| `eyes_mouth_prio=False` | 保持 src/dst 各三输出路径 | 真实 DataGenerator 启动 |
| `eyes_mouth_prio=True` | 第 4 个真实 EM mask 进入训练，缺失立即失败 | 真实训练 loss 路径 |
| AdaBelief / RMSprop | NumPy roundtrip 与 save/resume smoke 通过 | TensorFlow session slot 恢复 |
| Lion v2 | 新 state 可恢复，legacy state 不静默污染 | 旧模型恢复后短训 |
| FP32 | Batch 1 validated baseline | CUDA / cuDNN 真实训练 |
| FP16 / BF16 | experimental / blocked | GPU dtype、loss scale、保存恢复 |
| finite gradient gate | non-finite step 跳过 update | 真实图执行和日志 |
| Merge 默认路径 | dummy predictor smoke 通过 | 真实模型和图片 Merge 质量 |
| CLI | 未新增必须参数 | 后续新 flag 必须默认关闭 |

---

## 6. 测试与验证

macOS 本机验证通过：

```bash
python3 -m unittest discover -s tests/smoke -p 'test_batch1_*.py' -v
python3 -m tools.smoke.batch1_mac_smoke --print-json
python3 -m py_compile core/leras/training_save_resume_smoke.py tools/smoke/batch1_mac_smoke.py core/leras/precision_contract.py core/leras/optimizer_roundtrip.py core/enhancements/config.py tests/smoke/test_batch1_config_defaults.py tests/smoke/test_batch1_eyes_mouth_masks.py tests/smoke/test_batch1_finite_gradient_gate.py tests/smoke/test_batch1_mac_smoke.py tests/smoke/test_batch1_merge_default_path.py tests/smoke/test_batch1_optimizer_roundtrip.py tests/smoke/test_batch1_precision_contract.py tests/smoke/test_batch1_training_save_resume_smoke.py
```

结果：

```text
73 tests passed
Batch 1 smoke status: pass
syntax scan: 177 Python files, 0 errors
Python: 3.12.8
platform: Darwin arm64
numpy: 2.4.4 available
tensorflow: missing
cv2: missing
core.leras.nn: missing colorama
models / merger.MergeMasked: missing cv2
training_save_resume: pass
optimizers: adabelief, rmsprop, lion
max_abs_reload_error: 0.0
max_abs_update_error: 0.0
```

---

## 7. Windows GPU 待验证

仍需在 Windows GPU 环境补证：

- Windows 启动脚本与环境激活。
- CUDA / cuDNN / TensorFlow GPU discovery。
- SAEHD FP32 最小模型初始化、短训和 finite losses。
- `eyes_mouth_prio=False` 三输出路径与 `eyes_mouth_prio=True` 四输出路径真实 DataGenerator 启动。
- finite gradient gate 在真实 TensorFlow 图中的跳过 update 行为。
- Lion legacy state 恢复、warning / reset 与后续短训稳定性。
- 真实训练保存、进程退出或 session reset、重载、继续 step。
- 默认 Merge 真实模型与真实图片输出质量。
- FP16 / BF16 低精度稳定性、loss scale 与保存恢复。

---

## 8. 当前 Frontier

下一轮建议：

```text
进入 Batch 2 前置设计 / 任务拆分
```

建议先从以下方向开始，而不是直接写大功能：

- 固化 Batch 2 的 ticket 边界；
- 明确哪些功能只做默认关闭骨架；
- 将 Windows GPU 验证清单转成可执行 checklist；
- 保持 Batch 1 冻结边界：默认关闭、旧配置兼容、三输出默认路径、legacy state 保护、FP16/BF16 experimental。

---

## 9. 注意事项

- 不要把 macOS lightweight smoke 写成真实 GPU 训练或真实 Merge 质量已验证。
- 不要把 FP16 / BF16 写成 validated。
- Ticket 07 的 skipped update / zero loss 表示 finite gate 防护触发，不代表训练质量提升。
- 后续 Batch 2 不得同时引入 Region / Boundary / Frequency Loss、Dataset Metadata、Identity Geometry、Source Shape Template、Shape-aware Merge、UI 或服务化改造。
- 本轮尚未 push。push 前必须遵守仓库规则：只在用户明确确认后执行，且默认不推文档 commit。
