# 交接文档 (Handoff Document)

**创建时间**: 2026-07-28 16:10:30 (已于 16:18 补充深化说明)
**任务主题**: DeepFaceLab 后端 `--options-json` 训练超参数静默透传设计与任务拆解

---

## 1. 对话与设计要点总结

在本次会话中，我们针对 `deep-face-lab-tf2.x` 后端项目如何与 DFL GUI 客户端对接展开了深入分析与设计讨论：

1. **需求对接分析**：
   - 深入审查了 [dfl_backend_options_json_prd.md](file:///T:/deep-face-lab-gui/docs/dfl_backend_options_json_prd.md) 需求说明书。
   - 确认了 PRD 中列出的 27 项超参数（`batch_size`, `target_iter`, `optimizer`, `precision`, `random_warp`, `gan_power` 等）与 `deep-face-lab-tf2.x` 后端 `ModelBase.py` 及 `SAEHD` 模型中的参数定义 100% 对齐。

2. **核心设计决策明确**：
   - **区分两类参数**：明确区分了“模型神经网络架构参数”（如 `resolution`, `archi`, `ae_dims` 等，属于模型建立后硬编码不可变属性）与“运行期训练超参数”（27 项控制训练迭代的参数）。
   - **保持首次运行 (`is_first_run`) 原状**：在模型首次创建时仍保留控制台交互/模型结构初始化逻辑，严禁外部通过 `--options-json` 修改不可变结构。
   - **静默覆盖与防倒计时停顿**：在已有模型继续训练时，通过 `--options-json` 覆盖 `self.options` 字典，并重写 `ask_override()` 跳过 60 秒倒计时。

3. **产物沉淀与规格深化**：
   - 已在后端 Batch 1 目录下生成并全面丰富了功能 PRD 规范：[12-options-json-prd.md](file:///t:/deep-face-lab-tf2.x/.scratch/batch1-correctness-foundation/12-options-json-prd.md)。
   - 已将需求垂直切片拆解为 3 个包含伪代码、数据类型表与验证步骤的高质量 Issue 任务：
     * [Issue 12: CLI 参数扩展与透传链路](file:///t:/deep-face-lab-tf2.x/.scratch/batch1-correctness-foundation/issues/12-cli-options-json-arg.md)
     * [Issue 13: 后端 JSON 解析注入与选项覆写](file:///t:/deep-face-lab-tf2.x/.scratch/batch1-correctness-foundation/issues/13-options-json-parser-override.md)
     * [Issue 14: 倒计时防拦截与静默启动集成](file:///t:/deep-face-lab-tf2.x/.scratch/batch1-correctness-foundation/issues/14-options-json-silent-start-qa.md)

---

## 2. 下一步关注点

下一个接手的 Agent 或开发者应当关注：
1. **依序履约 Issues**：从 [Issue 12](file:///t:/deep-face-lab-tf2.x/.scratch/batch1-correctness-foundation/issues/12-cli-options-json-arg.md) 开始，按照 12 -> 13 -> 14 的依赖顺序依次领取任务并编写代码。
2. **代码修改点规范**：
   - [main.py](file:///t:/deep-face-lab-tf2.x/main.py): 添加 `--options-json` parser 参数及 `process_train` 字典传递。
   - [mainscripts/Trainer.py](file:///t:/deep-face-lab-tf2.x/mainscripts/Trainer.py): 增加 `options_json` 形参并透传给模型构造函数。
   - [models/ModelBase.py](file:///t:/deep-face-lab-tf2.x/models/ModelBase.py): 实现 `load_train_step_config()` 解析覆写逻辑，并重写 `ask_override()` 防倒计时停顿。

---

## 3. 建议技能 (Recommended Skills)

建议下一次会话接手的 Agent 使用以下技能：
- `triage`: 用于管理和领取新增的 Issues 任务。
- `tdd`: 在实现 `load_train_step_config()` 的 JSON 转换与覆写逻辑时使用红-绿-重构测试驱动开发。
- `review`: 在代码开发完成后审查是否完全符合规范与契约。
